# 🏝️ Живая карта Сицилии

Интерактивный веб-сайт для дизайнерского конкурса, представляющий остров Сицилия через живую карту с параллакс-эффектами, анимациями и модальными галереями.

## 🎯 Концепт

Остров оживает при скролле — море двигается, города дышат. Одностраничное скролл-сторителлинг приложение с интерактивной картой OpenStreetMap, где каждый регион рассказывает свою историю через маркеры и модальные галереи.

## 🛠️ Технологический стек

- **HTML5** — семантическая разметка
- **CSS3** — Custom Properties, Grid, Flexbox, современные функции
- **JavaScript ES Modules** — модульная архитектура
- **GSAP 3 + ScrollTrigger** — анимации и параллакс
- **Leaflet.js** — интерактивная карта OpenStreetMap
- **OpenStreetMap** — картографические тайлы

**Минимальные зависимости** — только GSAP и Leaflet через CDN, никаких фреймворков или сборщиков.

## 🚀 Быстрый старт

### ⚠️ ВАЖНО: Не открывайте index.html напрямую!

ES Modules требуют HTTP-сервер. При открытии через `file://` вы получите CORS ошибку.

### Самый простой способ (Windows)

**Двойной клик на `start.bat`** — сервер запустится автоматически и откроет браузер.

### Альтернативные способы запуска

**Вариант 1: Node.js (http-server)**
```bash
npx http-server . -p 8080
```
Затем откройте: http://localhost:8080

**Вариант 2: Python**
```bash
# Python 3
python -m http.server 8080

# Python 2
python -m SimpleHTTPServer 8080
```

**Вариант 3: PHP**
```bash
php -S localhost:8080
```

**Вариант 4: VS Code Live Server**
- Установите расширение "Live Server"
- Правый клик на `index.html` → "Open with Live Server"

**Вариант 5: macOS/Linux**
```bash
chmod +x start.sh
./start.sh
```

### Требования

- Любой современный браузер (Chrome 90+, Firefox 88+, Safari 14+, Edge 90+)
- Node.js (для `npx`) или Python/PHP (для альтернативных серверов)

## 📁 Структура проекта

```
.
├── index.html              # Главная страница
├── styles/
│   ├── main.css           # Основные стили, палитра, типографика
│   ├── map.css            # Стили карты, пинов, tooltip
│   └── modal.css          # Стили модального окна и галереи
├── scripts/
│   ├── app.js             # Главный модуль, инициализация
│   ├── gsap-setup.js      # Настройка GSAP и ScrollTrigger
│   ├── parallax.js        # Параллакс Hero-секции
│   ├── map.js             # Интерактивная карта
│   ├── modal.js           # Модальная система с галереей
│   └── animations.js      # Скролл-анимации маршрутов и контента
├── assets/
│   ├── svg/
│   │   └── sicily.svg     # Интерактивная SVG-карта
│   ├── img/               # Изображения (добавьте свои!)
│   └── icons/
│       └── favicon.svg    # Иконка сайта
├── data/
│   └── locations.json     # Данные о регионах
└── public/
    └── manifest.webmanifest  # PWA манифест
```

## 🎨 Кастомизация

### Изменение цветовой палитры

Отредактируйте CSS-переменные в `styles/main.css`:

```css
:root {
  --terracotta: #E67E22;  /* Акцент/CTA */
  --sea: #3498DB;         /* Море/небо */
  --olive: #95A472;       /* Регионы/маршруты */
  --sun: #F39C12;         /* Hover/подсветки */
  --ink: #0B0F1A;         /* Текст/тени */
  --paper: #FAF6F2;       /* Фон контента */
}
```

### Добавление нового региона

**1. Добавьте данные** в `data/locations.json`:

```json
{
  "id": "новый-регион",
  "name": "Название",
  "summary": "Подробное описание региона...",
  "gallery": ["./assets/img/регион-1.jpg", "./assets/img/регион-2.jpg"],
  "coords": { "lat": 37.5, "lng": 14.0 }
}
```

**2. Найдите координаты** региона (широта/долгота):
- Используйте [OpenStreetMap](https://www.openstreetmap.org/) для поиска координат
- Или [Google Maps](https://www.google.com/maps) — правый клик → "Что здесь?" → координаты

**3. Добавьте изображения** в `assets/img/`.

Маркер автоматически появится на карте при следующей загрузке страницы.

### Добавление маршрутов (опционально)

Для добавления линий маршрутов на карту можно использовать Leaflet Polyline:

В `scripts/map.js` добавьте после инициализации карты:

```javascript
const route = L.polyline([
  [37.5, 14.0],  // Начальная точка
  [38.0, 14.5]   // Конечная точка
], {
  color: '#E67E22',
  weight: 3
}).addTo(map);
```

### Изменение анимаций

Отредактируйте `scripts/animations.js`:

```javascript
// Изменить длительность
duration: 1.5,

// Изменить easing
ease: 'power3.out',

// Изменить trigger point
start: 'top center',
end: 'bottom center'
```

Параметры параллакса в `scripts/parallax.js`:

```javascript
// В HTML: data-speed="0.3"
// Больше значение = быстрее движение
```

## 🖼️ Работа с изображениями

### Добавление изображений

1. Поместите файлы в `assets/img/`
2. Обновите пути в `data/locations.json`
3. Используйте оптимизированные форматы (AVIF/WebP с JPEG-фолбеком)

### Рекомендуемые размеры

- **Истории**: 1200×675px (16:9)
- **Галереи**: 1600×900px или больше
- **OG-изображение**: 1200×630px

### Оптимизация

Используйте инструменты:
- [Squoosh](https://squoosh.app/)
- ImageOptim (macOS)
- TinyPNG/TinyJPG

Целевой размер файла: 100-300 KB на изображение.

## ♿ Доступность (a11y)

### Реализованные функции

✅ Семантическая HTML-разметка  
✅ ARIA-атрибуты (labels, roles, live regions)  
✅ Видимые focus-индикаторы  
✅ Skip-link для перехода к контенту  
✅ Клавиатурная навигация (Tab, Enter, Space, Esc, стрелки)  
✅ Поддержка `prefers-reduced-motion`  
✅ Переключатель анимаций  
✅ Контраст ≥4.5:1 (WCAG AA)  
✅ Фокус-ловушка в модальных окнах  
✅ Валидация форм с доступными сообщениями об ошибках  

### Тестирование

```bash
# Lighthouse audit
npx lighthouse http://localhost:8080 --view

# axe DevTools (расширение браузера)
# Pa11y
npx pa11y http://localhost:8080
```

**Целевые показатели Lighthouse:**
- Performance: ≥90
- Accessibility: ≥95
- Best Practices: ≥90
- SEO: ≥90

## 🎯 Acceptance Criteria Чек-лист

### Функционал
- [x] Параллакс 3-слойный на Hero (sky, mountains, sea)
- [x] Отключаемый параллакс (переключатель + prefers-reduced-motion)
- [x] Hover/focus подсвечивает регионы на SVG-карте
- [x] Клик по региону/пину открывает модалку с галереей
- [x] 10 регионов с данными (легко добавить новые)
- [x] Анимация маршрутов (stroke-draw) при скролле
- [x] Клавиатурная навигация (Enter/Space/Tab/Esc)
- [x] Модальные галереи с навигацией (стрелки, клавиатура, swipe)
- [x] Форма контактов с валидацией

### Качество
- [x] Семантическая разметка
- [x] Видимые focus styles
- [x] Модульный код, CSS-переменные
- [x] Без ошибок в консоли
- [x] Адаптивный дизайн
- [x] Производительность (LCP ≤2.5s)

## 🐛 Отладка

### Включить маркеры ScrollTrigger

В `scripts/gsap-setup.js`:

```javascript
ScrollTrigger.defaults({
  markers: true  // Показывает trigger точки
});
```

### Консольные команды (localhost)

В консоли браузера:

```javascript
// Переключить анимации
window.sicilyApp.toggleMotion(false);

// Обновить ScrollTrigger
window.sicilyApp.refreshScrollTrigger();
```

### Проблемы с SVG-картой

1. Проверьте, что карта загрузилась: откройте `http://localhost:8080/assets/svg/sicily.svg`
2. Проверьте консоль на CORS-ошибки (используйте HTTP-сервер!)
3. Убедитесь, что `class="region"` установлен для всех регионов

## 📱 PWA

Сайт готов к установке как Progressive Web App:

1. Добавьте иконки в `assets/icons/` (см. `.gitkeep`)
2. Раскомментируйте service worker (опционально)
3. Деплой на HTTPS-хостинг

## 🚀 Деплой

### GitHub Pages

```bash
git init
git add .
git commit -m "Initial commit: Sicily Live Map"
git remote add origin https://github.com/eveiljuice/sicilysite.git
git branch -M main
git push -u origin main

# Включите GitHub Pages в настройках репозитория
# Settings → Pages → Source: main branch
```

### Netlify

```bash
# Drag & drop папку проекта на https://app.netlify.com/drop
# Или через Netlify CLI:
npx netlify-cli deploy --prod
```

### Vercel

```bash
npx vercel --prod
```

## 📊 Performance советы

### Оптимизировано

✅ `loading="lazy"` на всех изображениях  
✅ `decoding="async"` для неблокирующей декодировки  
✅ `content-visibility: auto` для невидимых секций  
✅ Минимальные зависимости (только GSAP)  
✅ ES Modules для code splitting  
✅ Debounced resize handlers  

### Дополнительно

- Используйте CDN для GSAP (уже настроено)
- Включите HTTP/2 на сервере
- Включите gzip/brotli сжатие
- Добавьте Service Worker для кеширования (опционально)

## 🔧 Расширение функционала

### Добавить фильтр регионов

В `scripts/map.js` добавьте функцию фильтрации по типу:

```javascript
export function filterRegions(type) {
  locationsData.forEach(location => {
    const pin = document.querySelector(`[data-id="${location.id}"]`);
    pin.style.display = location.type === type ? 'block' : 'none';
  });
}
```

### Добавить поиск

```javascript
export function searchLocations(query) {
  return locationsData.filter(loc => 
    loc.name.toLowerCase().includes(query.toLowerCase())
  );
}
```

### Мультиязычность

Создайте `data/locations-en.json`, `data/locations-it.json` и переключайте через языковой селектор.

## 📄 Лицензия

Проект создан для дизайнерского конкурса. Свободен для образовательного использования.

---

**TL;DR для запуска:**

```bash
npx http-server . -p 8080
```

Откройте http://localhost:8080

**Как проверить:**
1. Скролль Hero — параллакс работает
2. Наведи на регион карты — подсветка
3. Кликни регион — модалка открывается
4. Кликни "Анимации: Вкл" — всё останавливается
5. Заполни форму контактов — валидация срабатывает

